@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;800&display=swap');

/* --- Global Setup --- */
body { 
    font-family: 'Inter', sans-serif; 
    background-color: #f8fafc; 
    color: #334155; 
    transition: all 0.3s ease; 
}

/* Smooth Fade-in effect */
section { 
    animation: fadeIn 0.4s ease-out; 
}

@keyframes fadeIn { 
    from { opacity: 0; transform: translateY(5px); } 
    to { opacity: 1; transform: translateY(0); } 
}

/* --- Navigation & Buttons --- */
.btn { 
    padding: 10px 20px; 
    border-radius: 12px; 
    font-size: 14px; 
    font-weight: 600; 
    cursor: pointer; 
    transition: all 0.2s; 
    border: none;
}

.btn-primary { background-color: #ec4899; color: white; }
.btn-secondary { background-color: #f1f5f9; color: #64748b; }

/* Active state logic */
.btn-active { 
    background-color: #ec4899 !important; 
    color: white !important; 
    box-shadow: 0 4px 12px rgba(236, 72, 153, 0.2);
}

.btn:hover { filter: brightness(95%); transform: translateY(-1px); }

/* --- Inactive/Grayscale state for Menu Lab --- */
.grayscale-card {
    filter: grayscale(100%);
    opacity: 0.6;
    background-color: #f8fafc !important;
    border: 1px dashed #cbd5e1 !important;
}

/* --- Tables --- */
.table-container { overflow-x: auto; }
.data-table { width: 100%; border-collapse: collapse; font-size: 14px; }
.data-table th { 
    text-align: left; 
    padding: 12px; 
    background: #f8fafc; 
    color: #94a3b8; 
    font-weight: 600; 
    text-transform: uppercase; 
    font-size: 11px; 
}
.data-table td { padding: 16px 12px; border-bottom: 1px solid #f1f5f9; }

/* Custom Scrollbar for Menu selection */
#menuItems::-webkit-scrollbar { width: 6px; }
#menuItems::-webkit-scrollbar-thumb { background: #fbcfe8; border-radius: 10px; }
